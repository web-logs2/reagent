<head_bar title="Add" showSearch="{{false}}" />
<view class="kinds">
  <view wx:for="{{kinds}}" class="type-item" bindtap="onKindChange" data-type="{{item.type}}">
    <view class="iconfont {{item.icon}}"></view>
    <text class="type-item-text">{{item.text}}</text>
  </view>
</view>